snc_redis:
    clients:
        default:
            type: predis
            alias: metric
            options:
                profile: default
            dsn: redis://localhost
            logging: false
