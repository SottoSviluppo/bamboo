{% extends 'AdminCoreBundle::Common/formComponent.html.twig' %}


{% form_theme form 'AdminCoreBundle::Forms/fields.html.twig' %}


{% set elcodi_context = {
    entity_type: 'tax',
} %}


{% block content %}

	{% set formAction =
        tax.id
            ? path('admin_tax_update', { id: tax.id })
            : path('admin_tax_save')
     %}

    {{ form_start(form, { action: formAction }) }}
        <fieldset>
            <div class="grid">
                <div class="col-1-3">
                    <div class="box-none">
                        <h3 class="fw-n">{{ 'admin.general.section.basic.title'|trans }}</h3>
                        <p>{{ 'admin.general.section.basic.description'|trans }}</p>
                    </div>
                </div>
                <div class="col-2-3">
                    <div class="box">
                        <ol>
                            <li>
                                {{
                                    form_row(form.name, {
                                        label: 'admin.general.field.name.title',
                                        attr: {
                                            placeholder: 'admin.general.field.name.placeholder',
                                        }
                                    })
                                }}
                            </li>
                            <li>
                                {{
                                    form_row(form.description, {
                                        label: 'admin.general.field.description.title',
                                        attr: {
                                            placeholder: 'admin.general.field.description.placeholder',
                                        }
                                    })
                                }}
                            </li>
                            <li>
                                {{
                                    form_row(form.value, {
                                        label: 'admin.general.field.value.title',
                                        attr: {
                                            placeholder: 'admin.general.field.value.placeholder',
                                        }
                                    })
                                }}
                            </li>
                        </ol>
                    </div>
                </div>
            </div>


			{% if form.enabled is defined %}
            <div class="grid">
                <div class="col-1-3">
                    <div class="box-none mb-n pb-m">
                        <h3 class="fw-n">{{ 'admin.general.section.visibility.title'|trans }}</h3>
                        <p>{{ 'admin.general.section.visibility.description'|trans }}</p>
                    </div>
                </div>
                <div class="col-2-3">
                    <div class="box mb-n pb-n">
                        <ol class="grid pt-m">
                            <li class="col-1-2">
                                {{
                                    form_row(form.enabled, {
                                        label: 'ui.label.enabled',
                                    })
                                }}
                            </li>
                        </ol>
                    </div>
                </div>
            </div>
			{% endif %}
        </fieldset>

        <div class="grid fixed-bottom animated fade-in-up">
            <div class="col-1-3">
                &nbsp;
            </div>
            <div class="col-2-3">
                <button type="submit" class="button-primary button-fat pv-s mr-l" id="submit-page">{{ 'ui.forms.save'|trans }}</button>
                <a href="{{ path('admin_tax_list') }}">{{ 'ui.forms.cancel'|trans }}</a>
            </div>
        </div>
    {{ form_end(form) }}


{# {% if tax.id > 0 %}
<div class="box-background">
    <h3 class="fw-n">{{ 'admin.province.list'|trans }}</h3>
    <a href="{{ path('admin_province_new') }}?tax={{tax.id}}" class="button-primary">
        <i class="icon-plus"></i>
        <span>Nuovo</span>
    </a>
    <table class="table">
        <thead>
            <tr>
    			{% include 'AdminGeoBundle::AdminProvince/tableHead.html.twig' %}
            </tr>
        </thead>
        <tbody>
			{% for province in tax.provinces %}
        	<tr>
            	{% include 'AdminGeoBundle::AdminProvince/tableRow.html.twig' with {'entity': province} %}
            </tr>
			{% endfor %}
        </tbody>
    </table>
</div>
{% endif %}

 #}

{% endblock content %}
