{% extends 'AdminCoreBundle::layout.html.twig' %}


{% set elcodi_context = {
    entity_type: 'tax',
} %}


{% block breadcrumb %}

	{% include '@AdminCore/Common/breadcrumb.html.twig' with {
        breadcrumb: [
            { name: 'admin.communication.single'|trans, active: false },
            { name: 'admin.tax.plural'|trans, active: true },
        ],
    } %}

{% endblock breadcrumb %}


{% block header_actions %}

    <a href="{{ path('admin_tax_new') }}" class="button-primary">
        <i class="icon-plus"></i>
        <span>{{ 'admin.general.new'|trans }}</span>
    </a>

{% endblock header_actions %}


{% block content %}

    <div class="row">
        <div class="col-md-12">
            {{
                render(
                    path('admin_tax_list_component', {
                        page: page,
                        limit: limit,
                        orderByField: orderByField,
                        orderByDirection: orderByDirection,
                    })
                )
            }}
        </div>
    </div>

{% endblock content %}
