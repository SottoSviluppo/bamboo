{% extends 'AdminCoreBundle::layout.html.twig' %}


{% set elcodi_context = {
    entity_type: 'variant'
} %}


{% block body %}

    {% if variant.id %}
        {{
            render(path('admin_product_variant_edit_component', {
                productId: product.id,
                id: variant.id,
            }))
        }}
    {% else %}
        {{
            render(path('admin_product_variant_new_component', {
                productId: product.id,
            }))
        }}
    {% endif %}

{% endblock body %}
