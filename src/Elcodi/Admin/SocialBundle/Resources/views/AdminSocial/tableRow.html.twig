    <td>
        <a href="{{ path('admin_social_edit', { id: entity.id }) }}">
            <strong>{{ entity.name|truncate(20, '…') }}</strong>
        </a>
    </td>
    <td>
        <a href="{{ path('admin_social_edit', { id: entity.id }) }}">
            <strong>{{ entity.url|truncate(20, '…') }}</strong>
        </a>
    </td>
    <td>
        <a href="{{ path('admin_social_edit', { id: entity.id }) }}">
            <strong>{{ entity.position|truncate(20, '…') }}</strong>
        </a>
    </td>
    <td>
        <a href="{{ path('admin_social_edit', { id: entity.id }) }}">
            <strong>{{ entity.class|truncate(20, '…') }}</strong>
        </a>
    </td>
    <td>
        {% if entity.enabled %}
            <span class="label label-ok">{{ 'ui.label.enabled'|trans }}</span>
        {% else %}
            <span class="label">{{ 'ui.label.disabled'|trans }}</span>
        {% endif %}
    </td>

    {% include '@AdminCore/Tables/actions.html.twig' with {
        entity: entity,
        delete: not entity.persistent,
        class: 'social',
    } %}
